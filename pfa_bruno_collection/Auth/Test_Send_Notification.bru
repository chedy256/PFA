meta {
  name: Test Send Notification
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/auth/test-send
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Bruno Alert",
    "body": "Sent from Bruno API Client!"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8000
  token: test-bruno-user
}
