meta {
  name: Notification History
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/auth/notifications
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8000
  token: test-bruno-user
}
